# Esperanza-Mobile — repo sweep, 2026-08-29

Repo: `https://github.com/Upupapp/Esperanza-Mobile` (public)
Gated SHA: `37d40688db4568aeca94f9345b962d1f79d684cc` (main)
Method: cloned to `/Users/user/Esperanza-Mobile`, gates run in a **detached worktree** at that
SHA, not in the working tree. Toolchain: Flutter 3.47.0 / Dart 3.13.0, macOS arm64 (Apple M4).

## Gate results (all in the detached worktree, app dir `Esperanza-Mobile-App/`)

| Gate | Result |
|---|---|
| `flutter pub get` | PASS — but **changed 4 locked deps** (`test_api` 0.7.11→0.7.12, `vector_math` 2.2.0→2.4.2, +2). `pubspec.lock` is not reproducible as committed. |
| `flutter analyze` | **No issues found** (463.8s) |
| `flutter test` | **All tests passed — 450 tests, 33m 10s**, 52 test files |

Not run: no device/simulator run, no `flutter build ios`/`apk`. The repo's own spec (Section 9)
says the app has *never* been run on a device or emulator — the author worked on Windows with
neither an Android SDK nor an iOS toolchain. This Mac has both, so that gap is closeable.

## Repo shape

- Flutter app, 135 Dart files / ~27,900 LOC in `lib/`, 52 test files.
- 20 commits, one author (`jave@lguids.com.ph`), single `main` branch, no tags, **no CI**.
  Nothing gates `main`.
- Frontend-only by design and honestly labelled: **no HTTP client dependency at all**. Auth,
  requests, notifications and profile are simulated and persisted to `shared_preferences`.
  The login screen itself toasts "This app is a frontend simulation".
- `ESPERANZA_MOBILE_WEB_ALIGNMENT.md` is a genuinely good spec: it names the missing Web-Admin
  APIs rather than pretending they exist.

## Findings

### 1. HIGH — real residents' document scans are committed to a public repo
`Esperanza-Mobile-App/Reference_forms/Esperanza  File/` (66 MB, 84 files) contains 18 JPEG
document scans at 1414x2000 — **11 of them are named after individual residents** and 7 are
UUID-named — plus `Certified Copy of Marriage certificate.pdf` and three
`Barangay_Certification*.pdf`. Public since commit `2811b49` (2026-08-14).

> The individual names are **withheld from this file because this repository is public** —
> naming them here would publish exactly the index this finding exists to prevent. The
> unredacted list is in the owner's local copy, `Esperanza-Mobile-SWEEP-full-2026-08-29.md`.
> `ls` the directory above to see them if you have legitimate need.

These are reference forms for building the app — they do not belong in the shipped repo at all,
let alone a public one. Removing them from `HEAD` is not enough; they stay in history and in
every existing clone/fork. Fixing this properly means history rewrite + force-push + credential
rotation-equivalent (assume they have been fetched).

### 2. HIGH — the mock "demo" identity is a real resident's record
`lib/services/mock_catalog.dart:2572` states plainly that the verified demo account is a named
resident's **actual** Constituents record from the Web Admin's own mock data — id, name, email,
birthdate, civil status, occupation, address, household and family IDs all sourced from that
record, not fabricated. The bundled demo ID images under `assets/images/` print that person's
real details, and the same pattern applies to the other two seeded demo identities.
A public demo app should ship synthetic identities. (Names withheld — see finding 1.)

### 3. HIGH — a corrupt/incompatible saved session hangs the app on the splash forever
Every service's restore path does an unguarded `jsonDecode` + `fromJson`, and no service has a
`try`/`catch` around it:

- [citizen_session_service.dart:49-63](Esperanza-Mobile/Esperanza-Mobile-App/lib/services/citizen_session_service.dart#L49-L63)
- [requests_service.dart:74-92](Esperanza-Mobile/Esperanza-Mobile-App/lib/services/requests_service.dart#L74-L92)
- also `balita_service`, `master_file_service`, `notifications_service`, `resident_profile_service`

Those `fromJson`s decode enums with `Enum.values.firstWhere(...)` and **no `orElse`**
(`service_request.dart:210`, `announcement.dart:125`, `receipt.dart:61`, `attachment.dart:86`),
so any renamed/removed enum value, or any malformed payload, throws.

The throw happens inside a future started from the constructor, so it is unhandled: `_loading`
is never set false and `notifyListeners()` never fires. `AuthGate`
([main.dart:78](Esperanza-Mobile/Esperanza-Mobile-App/lib/main.dart#L78)) then renders a
`CircularProgressIndicator` **forever**. Only clearing app data recovers it.

This is not hypothetical for this codebase — it already ships five separate migrations for
persisted data that changed shape (`_migrateStaleDemoIdentity`, `_migrateSeniorCitizenIdCategory`,
`_migrateObsoleteTrackingLabels`, ...), all of which run *after* the `fromJson` that would throw.

Fix: wrap each `_restore()` body in `try`/`catch`, fall back to empty state, and give every
enum `firstWhere` an `orElse`.

### 4. MEDIUM — status vocabulary has drifted from the web platform
Canonical list in `Esperanza-Web-Platform-frontend/CLAUDE.md` (15 statuses) vs mobile's
`lib/theme/app_status.dart` (15 statuses):

- Mobile is **missing `Resubmitted`**, which is canonical on the web side (8 files) and which
  mobile's own `requests_service.dart:964` writes into `statusHistory` as a literal string.
  It renders fine today (the timeline prints the raw label), but
  `AppStatusX.fromLabel('Resubmitted')` silently returns **`AppStatus.draft`** — so any future
  route through `fromLabel` will show a resubmitted request as grey "Draft".
- Mobile **invents `Waiting Requirements`**, which appears in **zero** files across the whole
  web platform — against the "never invent new status labels" rule both CLAUDE.mds state.
  The code knows this: `requests_service.dart:278` lists it as an *obsolete* label and migrates
  it to `Under Review` on load.
- ...yet `help_support_screen.dart:213` still tells citizens their request "may also show other
  statuses, such as Under Verification, Processing, or **Waiting Requirements**" — a status the
  app deliberately migrates away from and can no longer display. User-visible copy defect.
- `ESPERANZA_MOBILE_WEB_ALIGNMENT.md` Section 1 documents a 14-status list containing
  "Ready for Release", which exists in *neither* codebase now. The doc is stale (last updated
  2026-08-11); `app_status.dart`'s own header comment also still says "the exact 14 status names"
  for a 15-value enum.

Design tokens, by contrast, are exact: all 23 navy/brand/gold hexes in `app_colors.dart` match
`resources/css/app.css` 1:1. That part of the spec's parity claim verifies.

### 5. MEDIUM — the repo root is a second, broken Flutter project
The root holds a full Flutter scaffold (`pubspec.yaml`, `.metadata`, `analysis_options.yaml`,
`esperanza_mobile.iml`) with **no `lib/` and no `assets/`** — yet its `pubspec.yaml` declares
4 image assets and 4 font files under `assets/`. It cannot build. The real app is entirely in
`Esperanza-Mobile-App/`. Opening the repo root in an IDE or CI gives you the broken one.
`README.md`, `.metadata`, `package-lock.json` and all three screenshots are byte-identical
duplicates across the two. Left over from commit `8ea7fbe` "Merge existing Esperanza Mobile
repository with local project".

### 6. MEDIUM — `AttachmentPicker` ships in no screen, but has tests and is named in the spec
`lib/widgets/attachment_picker.dart` is the only one of 135 lib files **unreachable from
`main.dart`** (import-graph walk). It is imported solely by three test files. Section 1 of the
alignment spec names it as the shipped port of `ui/file-picker.blade.php` ("real device picker,
not a mock") and Section 9 names it as the component providing camera/gallery/PDF upload — but
the live screens use `RequirementUploader` instead. So `attachment_picker_test.dart` is green
against code no user can reach. (Document upload itself *is* wired: `requirement_uploader.dart:91`
calls `pickDocumentProtected`.)

### 7. LOW — release builds are not shippable
- `android/app/build.gradle.kts:30` — `release` still signs with the **debug** keystore
  (untouched Flutter TODO). Cannot go to Play.
- No `DEVELOPMENT_TEAM` in `ios/Runner.xcodeproj/project.pbxproj`. Cannot go to App Store.
- iOS deployment target 13.0.

### 8. LOW — default scaffold metadata is still user-visible
- `AndroidManifest.xml:3` — `android:label="esperanza_mobile"` (iOS correctly says
  "Esperanza Mobile"). The Android home screen shows the snake_case package name.
- `web/manifest.json`, `web/index.html`, and both `README.md`s still say
  `esperanza_mobile` / "A new Flutter project."

### 9. LOW — `CLAUDE.md` is referenced by the code but is not in the repo
`lib/theme/app_status.dart:8` and `:22` cite "CLAUDE.md's Universal status system" as the
authority for status names. There is no `CLAUDE.md` anywhere in the repo, and it is not
gitignored — it was simply never committed. A fresh clone has no rules file. (The web platform
repo does have one, at `Esperanza-Web-Platform-frontend/CLAUDE.md`.)

### 10. LOW — stale analyzer exclusions
`analysis_options.yaml` excludes `design_reference/**` (not in the repo; gitignored) and its
comment points at `lib/widgets/magnetic_navbar_core.dart`, which does not exist. `5-icons/`
is excluded and does exist (one unused reference file).

### 11. LOW — profile photo and full resident profile persist unencrypted
`resident_profile_service.dart:321` base64-encodes the citizen's profile photo into
`shared_preferences` alongside birthdate, address, household and family data. `SharedPreferences`
is plaintext XML on Android. Acceptable for a demo with no backend; not acceptable once this
holds real resident data. `local_auth`-style app lock is already listed as recommended-not-built
in the spec's Section 6.

### Non-findings (checked, clean)
- Zero `TODO`/`FIXME`/`HACK` in `lib/`.
- No secrets, API keys or tokens anywhere in `lib/`.
- All 41 declared assets exist; no missing files.
- `flutter analyze` clean under `flutter_lints`.
- Design tokens exact-match the web platform.

## Environment note
Four `flutter_tester` processes on this Mac are **2 days old at 0% CPU**, plus two `flutter`
tool processes aged 1d 6h — orphans from earlier sessions, unrelated to this run. Worth killing.

## Redaction

This file is the **public** copy. Individuals' names are withheld; every count, path, date,
line reference and technical finding is unchanged. The unredacted copy lives outside the
repository at `/Users/user/Esperanza-Mobile-SWEEP-full-2026-08-29.md`.

## Untouched
Nothing was committed or pushed. This file is untracked. The clone's working tree is otherwise
identical to `37d4068`; the verify worktree under the scratchpad was removed after the run.

---

# Round 2 — Windows lane, same day (2026-08-29)

Re-swept and gated independently on the **Windows/Android lane** (the round above ran
on macOS). Base SHA `7368c194`, a fresh clone; `origin/main` had exactly one branch and
no tags, and the clone was byte-identical to it. Toolchain matches the Mac lane exactly:
**Flutter 3.47.0 / Dart 3.13.0**.

## The merge step was a no-op — and that is the finding

A filesystem-wide scan for `.git` directories found **29 repositories** on this machine
and exactly **one** Esperanza-Mobile clone: the one made for this sweep. There was no
local-only work to preserve, `HEAD...origin/main` was `0 0`, and `main` is the only
branch. Nothing to merge; the protocol's merge step is recorded as vacuous rather than
skipped silently.

## Gate — Windows lane

| Gate | Round 1 (macOS M4) | Round 2 (Windows) |
|---|---|---|
| `flutter analyze` | clean, 463.8 s | **clean, 72.7 s** |
| `flutter test` | 450 passed, 33 m 10 s | **450 passed, 61 s** |
| `flutter build apk --release` | not run | **succeeds, 261 s, 94.8 MB** |
| install + launch on a device | not run | **runs on a Pixel 8 emulator, no app-level error** |

The ~25× spread is the machine, not the project. The Mac's "budget ~8 min / ~33 min"
note is now a per-lane table in `CLAUDE.md` so a fast run is not mistaken for a skipped
one, and a slow one is not mistaken for a hang.

## Corrections to Round 1

**Finding 4 stands — but only against `origin/main`.** Checked first against a local
web-platform clone that was **64 commits stale**, which still contained
`Waiting Requirements` and `Ready for Release` and therefore made mobile look correct
and Round 1 look wrong. It was the stale checkout that was wrong. Against the real
remote, Round 1's list is right and `Resubmitted` really is absent from mobile.
`CLAUDE.md` now says to check the vocabulary against `origin/main` explicitly.

**Finding 4 also understates the drift.** The web's `badge.blade.php` styles **17**
labels on `origin/main` — the 15 in the prose list plus `Verified` and `Unverified`,
which are in neither that list nor mobile. This is not only a badge colour:
`CitizenSessionService.accessLevel` gates Guest/unverified/verified on
`fromLabel(acc.status) == AppStatus.approved`, so an account arriving as `Verified`
would read as **unverified and be locked out of Dokyu**. Latent, not live — mobile only
ever writes `Pending Review` and `Approved`, and there is no backend — but it is the
first thing to break when one lands.

**Finding 9 is resolved.** `CLAUDE.md` was committed in `7368c194`.

## New findings

### 12 — the app now builds AND runs on Android; Section 9 is obsolete
`flutter build apk --release` succeeds on this lane, and the resulting APK was installed
and launched on a **Pixel 8 emulator (Android 16, API 36)** — the first time this app has
ever run on a device. It reaches its onboarding screen and stays foregrounded, and
`logcat` filtered to the app's own pid shows **no `E/flutter`, no exception, no fatal**.
The artifact reports `application-label:'Esperanza Mobile'`, `targetSdkVersion:'36'`,
`versionCode='1'`.

So the alignment spec's Section 9 line — "this build environment has no Android SDK
cmdline-tools configured" — is no longer true of this lane. What is still owed is a run
on **physical hardware**; an emulator is not a phone.

Three things came out of the run:

- Gradle warns that **AGP 8.11.1** (Flutter wants ≥ 9.0.1) and **Kotlin 2.2.20** (wants
  ≥ 2.3.20) will soon be dropped. Not yet fatal.
- `Choreographer: Skipped 271 frames! The application may be doing too much work on its
  main thread` at startup — consistent with decoding the ~2 MB PNGs of finding 13.
  Startup jank, not a crash.
- iOS remains unbuilt from either lane.

### 13 — HIGH: the real-identity demo images are compiled into every binary
Finding 2 is worse than a repo-contents problem. `assets/images/` is **39 MB across 37
declared assets**, including the ~2 MB demo ID cards that print a real person's details.
Those are bundled into the APK, so the exposure travels with every distributed build,
not just with the public repo. (`Reference_forms/` is *not* declared as an asset and does
**not** ship — that one is repo-only.) This also explains the 94.8 MB fat APK; Play needs
an AAB regardless.

### 14 — LOW: five root files are committed with CRLF
`git ls-files --eol` reports `i/crlf` for `.gitignore`, `README.md`, `pubspec.yaml`,
`analysis_options.yaml` and `esperanza_mobile.iml` — all part of the broken leftover
root scaffold (finding 5). There is no `.gitattributes`, so EOL handling depends on each
machine's `core.autocrlf`. A `* text=auto` was drafted and **deliberately not committed**:
it renormalises those five untouched files end-to-end, which would bury a crash fix under
pure line-ending churn. Worth doing on its own, together with finding 5's cleanup.

### 15 — LOW/MEDIUM: the repo cannot be cloned into a normal-depth path on Windows
Found by trying to verify the push from a clean clone. `git clone` into a scratch
directory ~100 characters deep **checked out nothing** — all 504 files reported as
deleted, with git suggesting `git restore --source=HEAD :/`. The cause is Windows'
260-character `MAX_PATH` with `core.longpaths` unset (its default).

The longest tracked path is **210 characters**, and the top offenders are all in
`Reference_forms/` — e.g. a 210-char DILEEP guideline PDF under
`Reference_forms/Placeholders/Tulong/`. That leaves under 50 characters of headroom for
the clone directory. The working clone used for this sweep sits at a 43-character path
and fits with 7 characters to spare; anything deeper silently produces an empty tree
that looks like catastrophic deletion rather than a checkout failure.

Two mitigations, and the second is the real one:

- `git config --global core.longpaths true` on every Windows machine that touches this
  repo (per-machine, easy to forget, and does not help a fresh contributor).
- Remove `Reference_forms/` — which findings 1 and 13 already require on privacy
  grounds. Doing that drops the longest path to well inside `MAX_PATH` and makes this
  finding disappear as a side effect.

Verification was completed in a short path (`C:	mp\ec_verify`): clean clone, **0 dirty
files**, `flutter analyze` clean in 66.6 s, **458 tests passed**, and `flutter pub get`
left `pubspec.lock` untouched — confirming the lock is reproducible now. The only
post-gate churn is 7 generated plugin registrants that Flutter rewrites with LF;
`git diff --numstat` is empty for all of them, so they are line endings only, not
content (see finding 14).

## Fixed in this round

Finding 3 (the permanent splash hang) is fixed, with the fix proven rather than assumed.

- All **six** services (`citizen_session`, `requests`, `balita`, `master_file`,
  `notifications`, `resident_profile`) now guard `_restore()`. The load flag is flipped
  and `notifyListeners()` fired from a `finally`, so the gate opens on every path.
  Unreadable state is discarded rather than retained; `CitizenSessionService`
  additionally clears the poisoned key so the next launch starts clean.
- All four enum decoders (`PostMediaType`, `AttachmentCategory`, `ReceiptType`,
  `ServiceCategory`) got an `orElse`, so one unknown value costs one field, not the
  whole collection. No unguarded `values.firstWhere` remains in `lib/`.
- New `test/corrupt_persisted_state_recovery_test.dart` — 8 cases over three shapes of
  bad payload (not JSON, wrong type, unknown enum value).

**The new test was watched failing before it was trusted:** with the fix stashed, all
8 fail, each reporting `<Service> never finished loading — the splash would spin
forever`. With the fix applied, 8/8 pass. Every one of the six services really did hang.

Also fixed: the finding-4 copy defect (`help_support_screen.dart` no longer advertises
`Waiting Requirements`, which the app migrates away and can never display); finding 8's
Android half (`android:label` was the snake_case `esperanza_mobile` on the home screen —
iOS was already correct via `CFBundleDisplayName`); finding 10's stale comments; and the
non-reproducible `pubspec.lock` — the resolved 3.47.0 lock is now committed, so
`flutter pub get` no longer dirties the tree on either lane.

`CLAUDE.md` also no longer names only the Mac's rules path. Both lanes work this one
repo, so anything machine-specific has to name both or it silently excludes a lane.

## Still open — owner decisions, not agent fixes

Findings **1**, **2** and **13** (real residents' scans in a public repo; real identities
in the demo data and in every built binary) are untouched. They need history rewrite plus
force-push plus an assume-fetched response, and this repo's own rules say to raise them
with the owner rather than attempt a fix. Findings **5** (duplicate broken root scaffold),
**6** (`AttachmentPicker` unreachable), **7** (release signs with the debug keystore; no
iOS team), **11** (plaintext profile persistence) and **14** are also unchanged.
